@props(['number', 'label'])

<div class="text-center" data-reveal>
    <div class="font-display text-4xl font-extrabold">{{ $number }}</div>
    <div class="mt-1 font-semibold">{{ $label }}</div>
</div>
